# Codebase Patterns

- Board-driven build: read `.build-kit/.slices/current_context.json` for the active
  context, then `<context>/index.json` for slice statuses. Pick the highest-priority
  "Planned" slice, mark it InProgress (locally and on the board), build it end-to-end,
  then mark it Done. One slice per run.
- Slice detail comes from the eventmodelers `slicedata` API
  (`GET /api/org/:orgId/boards/:boardId/slicedata?contextId=<id>`), not the minimal
  cached `slice.json` — re-fetch and persist the full object before trusting local files.
- Never build manually: dispatch to `build-state-change`/`build-state-view`/
  `build-automation` per the slice's `sliceType`.
- `.build-kit/CLAUDE.md` "First-time project setup" must run once, before the first
  build skill, in a brand-new project: resolve `pyproject.toml` TODOs, then create
  `src/<pkg>/__init__.py`, `application.py`, `main.py`, `tests/integration/conftest.py`,
  and fill README/mkdocs/docs TODO titles.
- Read-model complexity guide: a "count of related entities" view (e.g. subscription
  count) does not need a `found`/404 concept — expose the count as a derived property
  (`len(self.items)`) over an always-defined zero state, matching specs that expect
  200 + zero values for an entity with no prior events, not a 404.

## 2026-08-12 — Student Course Subscriptions (STATE_VIEW)

- Picked slice `423a429c-b5fd-4b19-b111-3505b38471d0` ("Student Course Subscriptions"),
  the only "Planned" slice in context `course-subscriptions`. Marked InProgress in
  `index.json` and via the board's `update-slice-status`.
- `main` and `restart` are unrelated git histories (no merge-base); `main` holds a
  stale "Done" implementation that contradicts the live board's "Planned" statuses.
  Building on `restart` / `feature/studentcoursesubscriptions` instead of `main`.
- Ran first-time project setup (see Codebase Patterns above) — this is the first
  slice built in this project.
- Invoked `build-state-view` skill, on-demand approach (no board comment requested
  materialized). Consistency boundary tag: `student:{studentId}`.
- Added `StudentRegistered`, `StudentSubscribed`, `StudentUnsubscribed` to
  `src/course_subscriptions/course_subscriptions/events.py`.
- Route: `GET /students/{student_id}/course-subscriptions`.
- No lock files existed yet (first-time setup): ran the full
  "Regenerating lock files" procedure (`rm -rf requirements*`, `hatch env prune`,
  recreate every env) instead of trusting the partial set produced by ad hoc
  `hatch -e dev`/`hatch -e integration-tests` runs, to keep them consistent.
- Scoped checks green: style (ruff + pydoclint), types (pyrefly), acceptance +
  integration tests for this slice only. `docs/openapi.json` regenerated —
  contains exactly the one new path.
- No `test-analyzer` skill is installed in this project's `.claude/skills/` —
  skipped that step; nothing to invoke.
- Pre-commit's `hatch-unit-tests`/`hatch-documentation-tests` hooks failed with
  pytest exit 5 (no tests collected) since both suites were still empty from
  scaffolding. Added one real test each rather than a bare placeholder:
  `tests/unit/test_projection.py` covers `_tags()` (CLAUDE.md's own example of
  non-trivial `Slice` logic worth a unit test), and
  `tests/documentation/test_docs.py` runs `mktestdocs.check_md_file` over
  every `docs/**/*.md` + `README.md` — using the `mktestdocs` dependency for
  its stated purpose, so future doc code blocks are actually verified.
- Set slice status to Done in `index.json`, `slice.json`, and via the board's
  `update-slice-status` fallback curl. Committed as `feat: Student Course
  Subscriptions` on `feature/studentcoursesubscriptions` and fast-forward
  merged into `restart` (same lineage). `main` has an unrelated history and a
  stale "Done" implementation contradicting the live board — left untouched;
  flagged for a human to decide how to reconcile it (see AGENTS.md).

## 2026-08-12 — Unsubscribe Student (STATE_CHANGE)

- Picked slice `b6574a3c-7e60-4de8-b46d-d757f0ba4712` ("Unsubscribe Student"), the
  highest-priority "Planned" slice (index 0) in context `course-subscriptions`.
  Marked InProgress in `index.json`, `slice.json`, and via the board.
- Found `.build-kit/.slices/course-subscriptions/*/slice.json` and `index.json`
  truncated to bare `{id, title, status}` stubs in the working tree at session
  start, even for the already-`Done` "Student Course Subscriptions" slice whose
  own commit holds full detail — this was accidental corruption from an earlier
  session, not an intended format. Restored via `git checkout --` against the
  last commit before proceeding.
- Created branch `feature/unsubscribestudent` from `restart` (the branch holding
  live, board-aligned lineage — `main` and the old `feature/subscribestudent`/
  `feature/changecoursecapacity` branches are unrelated pre-restart histories;
  ignoring them per the established pattern, see AGENTS.md).
- Fetched full slice data via `get_slice_data` (contextId required alongside
  sliceId — `sliceId` alone errors). Command `Unsubscribe Student` (courseId,
  studentId, both idAttribute) -> event `Student Unsubscribed`. Specs need
  `Course Registered`, `Student Registered`, `Student Subscribed` as given
  events — per `.build-kit/CLAUDE.md`'s DCB testing notes, these only need to
  exist as `Decision` classes to seed via `app.events.append`, not as built
  commands, so this doesn't actually depend on Register Course/Register
  Student/Subscribe Student being implemented first.
- `StudentUnsubscribed`, `StudentRegistered`, `StudentSubscribed` events already
  exist in `events.py` (added for the earlier read-model slice). `CourseRegistered`
  does not exist yet — needs adding for the "given" clauses.
- Designed a dual-`Selector` consistency boundary: one single-tag selector on
  `course:{course_id}` for `CourseRegistered` (proves the course exists), one
  dual-tag selector on `[course:{course_id}, student:{student_id}]` for
  `StudentSubscribed`/`StudentUnsubscribed` (proves the current subscription
  state). The emitted `StudentUnsubscribed` carries both tags, satisfying
  Selector tags ⊆ trigger tags for both selectors at once.
- Created `src/course_subscriptions/command.py` (first command slice in the
  project) with `CommandOutcome`, `CommandSlice`, `CommandResponse`, and added
  the one documented `do()` override on `application.py` per
  `.build-kit/CLAUDE.md`'s *Command outcomes* section.
- Two-entity route address: per CLAUDE.md's "nest under the entity the command
  mutates; the other stays in the body" rule, chose `POST
  /students/{student_id}/unsubscriptions` with `course_id` in the body — student
  because the existing "Student Course Subscriptions" read model and this
  slice's own screen (lane "Student", inbound from that read model) both frame
  this as the student managing their own subscription list. Verified no
  collision with the one existing route (`GET
  /students/{student_id}/course-subscriptions`) via `hatch run docs:openapi`
  and grepping `docs/openapi.json`.
- The slice.json "Not subscribed" scenario arranges a *different* student's
  subscription to the same course, to prove per-student isolation — but GWT's
  `given().when()` rejects any given event outside the slice's own boundary
  tags (`AssertionError: Consistency boundary wouldn't have selected: ...`).
  Split the coverage: the acceptance test proves `not_subscribed` with a
  boundary-compliant history (course registered, nobody subscribed); the
  cross-student isolation case moved to the integration suite, seeding both
  students' histories directly via `app.events.append`, per CLAUDE.md's own
  note that this property "belongs in the integration suite."
- `TSlice` (application.py) and `Sequence` (slice.py) were both flagged by
  ruff `TCxxx` for being importable only under `TYPE_CHECKING` — neither is a
  Pydantic field type nor a FastAPI dependency parameter, so the general
  `from __future__ import annotations` + `TYPE_CHECKING` guard applied cleanly
  (the two documented exceptions in CLAUDE.md's FastAPI/Pydantic gotchas did
  not apply here).
- Ran `hatch run acceptance-tests:run`, `integration-tests:run`, `unit-tests:run`
  (all passing, 8+8+1), `hatch run style:check .` (clean), `hatch run
  types:check` (0 errors) before committing. No dependency changes in this
  slice, so no lock-file regeneration was needed.
- Slice complete: marked "Done" in `index.json`, `slice.json`, and via
  `update_slice_status` MCP. Merged `feature/unsubscribestudent` into `restart`.

## 2026-08-12 - Change Course Capacity (STATE_CHANGE) - iteration start

- Session start: found `.build-kit/.slices/course-subscriptions/*.json` (index.json
  and all per-slice slice.json files) truncated to bare `{id, title, status}` stubs
  in the working tree, matching the exact corruption pattern already documented in
  the previous entry. Restored via `git checkout -- .build-kit/.slices/course-subscriptions/`
  against the last commit (d1a4d44) before proceeding.
- Picked highest-priority Planned slice: "Change Course Capacity" (index 2,
  STATE_CHANGE). Marked InProgress in index.json, slice.json, and via
  `update_slice_status` MCP.
- Branch: `feature/changecoursecapacity` already existed but was pre-restart,
  unrelated history (no merge-base with `restart`) - same pattern as the other
  stale `feature/*` branches noted in AGENTS.md. Confirmed it was already an
  ancestor of `main` (zero data loss), deleted it, and recreated fresh from
  `restart`.
- Slice has an unresolved-looking comment on the command ("what happens when
  capacity drops below current subscriberCount?") but the specifications already
  answer it: a `SPEC_ERROR` scenario "Cannot reduce capacity below current
  subscriptions" rejects the command outright. Treating the comment as resolved
  by the spec, no ambiguity blocking implementation.
- Dispatching to `build-state-change` skill next.
- Consistency boundary: single `Selector(types=[CourseRegistered,
  CourseCapacityChanged, StudentSubscribed, StudentUnsubscribed],
  tags=[f"course:{course_id}"])` — tag intersection lets one selector
  aggregate subscription count across many students without a
  `Sequence[Selector]`. Documented as a new reusable pattern in `AGENTS.md`.
- Added `CourseCapacityChanged` to the shared
  `course_subscriptions/events.py`. Created the
  `change_course_capacity` package (`slice.py`, `routes.py`), wired
  `main.py`. Route: `POST /courses/{course_id}/capacity-changes` (nominalised
  intention noun over the `course:{course_id}` boundary tag), 201/204/422
  responses, tag/operation_id `change_course_capacity`.
- Acceptance tests (4): happy path, unknown course, same capacity rejected,
  capacity below current subscriptions rejected — all via GWT against the
  single selector above.
- Integration tests (5): 201 happy path, 422 unknown course, 422 same
  capacity, 422 below-subscriptions (seeded via `app.events.append`), 422
  missing field. Full integration suite (13 tests, all slices) green, no
  regressions.
- Scoped checks green: `style:check` (ruff + pydoclint), `types:check`
  (pyrefly, 0 errors), acceptance (12 total)/integration (13
  total)/unit (1)/documentation (2) suites all passing. No dependency
  changes in this slice, so no lock-file regeneration was needed.
- `docs/openapi.json` regenerated: purely additive diff (new
  `ChangeCourseCapacityRequest` schema + new
  `/courses/{course_id}/capacity-changes` path), no existing paths altered.
- Board sync: `mcp__eventmodelers__update_slice_status` (sliceId
  `9c31851a-1b99-4a50-b0ba-621599def7a6`) failed three times in a row with
  `Internal Server Error`, including on a plain `list_slices` diagnostic
  call — looks like a transient platform-side outage, not a malformed
  request. Local `index.json`/`slice.json` are already set to "Done"; a
  future iteration or human should retry the board sync for this slice.
- Slice complete: marked "Done" in `index.json` and `slice.json`. Committed
  as `feat: Change Course Capacity` on `feature/changecoursecapacity` and
  fast-forward merged into `restart`.

## 2026-08-12 - Register Course (STATE_CHANGE) - iteration start

- Session start: found `.build-kit/.slices/course-subscriptions/*.json` (index.json
  and all per-slice slice.json files) truncated to bare `{id, title, status}` stubs
  in the working tree again - same corruption pattern as prior iterations. This time
  restored via the board's `/slicedata` API (contextName=Course Subscriptions)
  rather than `git checkout`, since HEAD's committed version could itself be stale
  relative to the board (see next point).
- Discovered the previous iteration's board sync for "Change Course Capacity" had
  failed (documented in progress.txt as a transient Internal Server Error), so the
  live board still showed it as "InProgress" while the local commit already said
  "Done". Restoring straight from the board API would have silently regressed the
  local status back to InProgress. Fixed by keeping "Done" locally and retrying the
  `update_slice_status` sync (curl fallback, no MCP tools visible this session) -
  succeeded this time, board now shows "Done" too.
- Picked highest-priority Planned slice: "Register Course" (index 3, STATE_CHANGE).
  Marked InProgress in index.json, slice.json, and via curl fallback node:changed
  event (sliceStatus).
- No pre-existing `feature/registercourse` branch. Creating fresh from `restart`
  (per AGENTS.md: `restart` is the branch with the live board-tracking lineage,
  not `main` - do not merge into or rebase onto `main` without asking a human).
- Dispatching to `build-state-change` skill next.
- `CourseRegistered` already existed in `events.py` from an earlier slice - no new
  event type needed, only a new slice package.
- Consistency boundary tag: `course:{course_id}`, create-once invariant
  (`self.registered = False/True`). Command has no id to nest under (it creates the
  entity), so per the API addressing rule the route sits at root: `POST
  /course-registrations`.
- Files added: `register_course/{__init__.py,slice.py,routes.py}`,
  `tests/acceptance/course_subscriptions/register_course/test_register_course.py`,
  `tests/integration/course_subscriptions/test_register_course.py`. Wired
  `register_course_routes.router` into `main.py`. Regenerated `docs/openapi.json`
  (purely additive: one new path `/course-registrations` + `RegisterCourseRequest`
  schema).
- No unspecified zero-capacity validation added: the slice's command has an
  unresolved board comment about zero-capacity but no corresponding SPEC_ERROR, so
  (per the precedent set in the Change Course Capacity iteration) only the two
  specified scenarios were implemented.
- Scoped checks green: style (ruff + pydoclint), types (pyrefly), acceptance (14
  passed), integration (16 passed), plus full unit-tests (1 passed) as a
  nice-to-have. No dependency changes, so no lock-file regeneration needed.
- Slice complete: marked "Done" in `index.json` and `registercourse/slice.json`.
  Committed as `feat: Register Course` on `feature/registercourse` and
  fast-forward merged into `restart`.

## 2026-08-12 - Course Catalogue (STATE_VIEW) - iteration start

- Restored `.build-kit/.slices/course-subscriptions/` from the last commit: found
  the same modified-in-working-tree corruption pattern noted in every prior
  iteration (all slice.json/index.json truncated to bare stubs). `git checkout --`
  fixed it.
- Picked highest-priority Planned slice: "Course Catalogue" (index 4, id
  `d30964fa-e955-4edd-a3bb-25e956575b0d`, STATE_VIEW).
- **New bug found (not the usual corruption)**: two board slices share the folder
  name `coursecatalogue` - this Planned one (`d30964fa...`) and a `Blocked`
  duplicate (`fac33170...`, index 7, extensively documented in its own `comments`
  as a leftover/incomplete duplicate). The on-disk `coursecatalogue/slice.json`
  has held the *Blocked* slice's definition since the very first commit
  (2e2764e) - a `load-slice` folder-collision bug, not working-tree corruption.
  `index.json`'s inline `definition` for `d30964fa...` was correct/complete the
  whole time. Fixed by overwriting `coursecatalogue/slice.json` with the correct
  Planned definition from `index.json` (546 lines: 1 readmodel, 2 screens
  (Student lane, Course Manager lane), 4 specifications).
- Marked InProgress in `index.json` and `coursecatalogue/slice.json`. Board sync
  via curl fallback (`node:changed`/`sliceStatus`) on node id
  `d30964fa-e955-4edd-a3bb-25e956575b0d` succeeded directly - skipped the
  title-based lookup in `update-slice-status`'s Step 2 since two slices share the
  title "Course Catalogue" and a name match would be ambiguous; used the known
  node id directly instead.
- Branch: no pre-existing `feature/coursecatalogue` branch. Created fresh from
  `restart`.
- Read model: `courseId` (idAttribute), `title`, `capacity`, `numberOfSubscriptions`
  (generated/derived), `listElement: true` -> a collection view across all
  courses, not a single-entity lookup. Inbound: `CourseRegistered`,
  `CourseCapacityChanged`, `StudentSubscribed`, `StudentUnsubscribed` (all four
  already exist in `events.py` from earlier slices). Outbound: two HTML screens
  (Student lane, Course Manager lane) - both consume the same read model, no
  extra fields needed per screen.
- 4 specifications: courses shown with title/capacity; empty catalogue (200 +
  empty list, not 404 - matches the read-model complexity guide); capacity
  changes reflected; subscription count derived from Subscribed minus
  Unsubscribed across multiple students.
- Dispatching to `build-state-view` skill next.
- Route: `GET /course-catalogue` (collection view, root path, no query params -
  matches all 4 specs). Global consistency boundary `Selector(types=[...4
  events...], tags=[])`, justified in the projection docstring - the catalogue
  spans every registered course, so there is no single entity tag to scope to.
  Confirmed via `eventsourcing.dcb.gwt.selector_matches`/`api.py` docstrings that
  `tags=[]` is vacuously satisfied by every event (`not selector.tags or
  set(selector.tags).issubset(event.tags)`), not "matches nothing" - safe for
  both GWT and the real recorder.
- Multi-entity read model per the `build-state-view` complexity guide's rare
  case: `self.courses: dict[str, CourseCatalogueEntry] = {}` where
  `CourseCatalogueEntry` (a small `@dataclass`) holds `title`, `capacity`, and a
  `subscribers: set[str]` (add on `StudentSubscribed`, discard on
  `StudentUnsubscribed`); `number_of_subscriptions` in the response is
  `len(entry.subscribers)` - a derived property, not stored state.
  `CourseCapacityChanged` updates `entry.capacity` in place.
- Files added: `course_catalogue/{__init__.py,projection.py,routes.py}`,
  `tests/acceptance/course_subscriptions/course_catalogue/test_course_catalogue.py`
  (4 tests: empty catalogue, title/capacity, capacity change, subscription
  count net of unsubscribe), `tests/integration/course_subscriptions/test_course_catalogue.py`
  (5 tests, incl. multi-course isolation - two independently-registered courses
  each keep their own entry). Wired `course_catalogue_routes.router` into
  `main.py`. Regenerated `docs/openapi.json` (purely additive: one new path
  `/course-catalogue` + two new schemas).
- No test-analyzer skill installed in this project (same as every prior
  iteration) - skipped that step.
- Scoped checks green: style (ruff + pydoclint), types (pyrefly, 0 errors),
  acceptance (18 total, 4 new)/integration (21 total, 5 new)/unit (1)/
  documentation (2) suites all passing. No dependency changes, so no lock-file
  regeneration needed.
- Board sync via curl fallback succeeded for both InProgress and Done
  transitions, using the known node id directly rather than the
  `update-slice-status` skill's title-based lookup (ambiguous: two board slices
  share the title "Course Catalogue" - see the folder-collision bug noted
  above).
- Slice complete: marked "Done" in `index.json` and `coursecatalogue/slice.json`.
  Committing as `feat: Course Catalogue` on `feature/coursecatalogue` and
  fast-forward merging into `restart`.

## 2026-08-12 - Subscribe Student (STATE_CHANGE) - iteration start

- Session start: found `.build-kit/.slices/course-subscriptions/*.json` truncated to
  bare stubs in the working tree again - same corruption pattern as every prior
  iteration. Restored via `git checkout -- .build-kit/.slices/course-subscriptions/`.
- Picked highest-priority Planned slice: "Subscribe Student" (index 5, id
  `0aaedf9d-e5d0-4bc6-b2d2-d3f6e6dd1945`, STATE_CHANGE). The only other Planned
  slice, "Register Student" (index 6), is an AUTOMATION triggered by a "Student to
  Register" readmodel that doesn't exist yet (status "Created") - lower priority by
  index anyway, so no conflict in the pick.
- Branch `feature/subscribestudent` existed but was pre-restart, unrelated history
  (no merge-base with `restart`, already an ancestor of `main`) - same pattern as
  other stale `feature/*` branches. Deleted and recreated fresh from `restart`.
- Marked InProgress in `index.json`, `subscribestudent/slice.json`, and via the
  board's `node:changed`/`sliceStatus` curl fallback (succeeded first try).
- Command `Subscribe Student` (courseId, studentId) -> event `Student Subscribed`.
  Board comment flags ambiguity about the subscription limit (2 vs 10 vs 5 in
  various references) - resolved by the spec itself: `courseLimit` is a per-student
  field set at registration (see "Student at subscription limit (2)" spec, which
  seeds a `Student Registered` event with `courseLimit: 2`), not a hardcoded
  constant. No blocking ambiguity.
- 5 specifications: happy path, unknown course (SPEC_ERROR), course full
  (SPEC_ERROR), already subscribed (SPEC_ERROR), student at subscription limit
  (SPEC_ERROR, needs count of student's active subscriptions across courses vs
  their courseLimit).
- Designed a `Sequence[Selector]` boundary with two independent selectors:
  `course:{course_id}` (for `CourseRegistered`/`CourseCapacityChanged` plus
  `StudentSubscribed`/`StudentUnsubscribed` scoped to that course) and
  `student:{student_id}` (for `StudentRegistered` plus
  `StudentSubscribed`/`StudentUnsubscribed` scoped to that student). A single
  `StudentSubscribed`/`StudentUnsubscribed` event can satisfy both selectors at
  once (this student's subscription to this course), so the event handlers use
  independent `if`s rather than `elif` to update each of the three counters
  (`course_subscription_count`, `student_subscription_count`,
  `already_subscribed`) that its tags actually match - documented as a new
  reusable pattern in `AGENTS.md`, distinct from Change Course Capacity's
  single-selector tag-intersection pattern (already documented there).
- Route: `POST /students/{student_id}/subscriptions` (nested under student, per
  precedent from Unsubscribe Student's `/students/{student_id}/unsubscriptions`;
  `course_id` travels in the body). Verified no collision via `grep` on
  `docs/openapi.json` before writing the route.
- Files added: `subscribe_student/{__init__.py,slice.py,routes.py}`,
  `tests/acceptance/course_subscriptions/subscribe_student/test_subscribe_student.py`
  (5 tests, one per specification), `tests/integration/course_subscriptions/test_subscribe_student.py`
  (6 tests: 201 happy path, 422 unknown course, 422 already subscribed, 422
  course full, 422 subscription limit reached, 422 missing field). Wired
  `subscribe_student_routes.router` into `main.py`. Regenerated
  `docs/openapi.json` (purely additive: one new path
  `/students/{student_id}/subscriptions` + `SubscribeStudentRequest` schema).
- One `ruff` E501 (docstring line too long) on `consistency_boundary`'s
  docstring - fixed by shortening the wording, no logic change.
- No `test-analyzer` skill installed in this project (same as every prior
  iteration) - skipped that step.
- Scoped checks green: style (ruff + pydoclint, clean), types (pyrefly, 0
  errors), acceptance (23 total, 5 new)/integration (27 total, 6 new) suites
  passing. No dependency changes, so no lock-file regeneration needed.
- Board sync via curl fallback succeeded for both InProgress and Done
  transitions on node id `0aaedf9d-e5d0-4bc6-b2d2-d3f6e6dd1945`.
- Slice complete: marked "Done" in `index.json` and
  `subscribestudent/slice.json`. Committing as `feat: Subscribe Student` on
  `feature/subscribestudent` and fast-forward merging into `restart`.
